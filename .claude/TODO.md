# TODO

## Now (Week 1 — skeleton, mock data)
- [ ] Create screen wrapper component, "ScreenWrapper.tsx", that will be used as the root component of every screen.
    Give it:
    - scrollview component
    - keyboard avoiding view component
    - spread children from props into the inner most JSX
- [] Apply ScreenWraper to index.tsx
